# Connector setup check

Temporary file. Delete this branch and close the PR once reviewed.

Verifies the automation path end to end:

- [x] MCP connector installed on this repo only
- [x] Branch created from `master`
- [x] File written to a `claude/` branch
- [x] Pull request opened against a protected `master`

If you are reading this in a pull request, the path works.
